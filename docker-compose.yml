services:
  postgres:
    container_name: postgres
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=orders
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  topic-register:
    container_name: topic-register
    build:
      context: .
      dockerfile: Schemas/TopicRegister/Dockerfile
    environment:
      - DOTNET_ENVIRONMENT=Development
      - Kafka__BootstrapServers=broker:29092
    depends_on:
      broker:
        condition: service_started
    restart: "no"

  schema-register:
    container_name: schema-register
    build:
      context: .
      dockerfile: Schemas/SchemaRegister/Dockerfile
    environment:
      - DOTNET_ENVIRONMENT=Development
      - Kafka__BootstrapServers=broker:29092
      - Kafka__SchemaRegistryUrl=http://schema-registry:8081
    depends_on:
      topic-register:
        condition: service_completed_successfully
      schema-registry:
        condition: service_started
    restart: "no"

  fulfillment-service:
    container_name: fulfillment-service
    build:
      context: .
      dockerfile: FulfillmentService/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_URLS=http://+:8080
      - Kafka__BootstrapServers=broker:29092
      - Kafka__SchemaRegistryUrl=http://schema-registry:8081
      - GROUP_ID=test-1
    ports:
      - "5054:8080"
    depends_on:
      schema-register:
        condition: service_completed_successfully

  order-service:
    container_name: order-service
    build:
      context: .
      dockerfile: OrderService/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_URLS=http://+:8080
      - Kafka__BootstrapServers=broker:29092
      - Kafka__SchemaRegistryUrl=http://schema-registry:8081
      - ConnectionStrings__OrdersDatabase=Host=postgres;Port=5432;Database=orders;Username=postgres;Password=postgres
    ports:
      - "5295:8080"
    depends_on:
      postgres:
        condition: service_healthy
      schema-register:
        condition: service_completed_successfully

  flink-jobmanager:
    image: flink:1.18-java17
    container_name: flink-jobmanager
    hostname: flink-jobmanager
    ports:
      - "8084:8081"
    depends_on:
      broker:
        condition: service_started
    environment:
      - FLINK_ENV=java17
      - FLINK_PROPERTIES=jobmanager.rpc.address:flink-jobmanager
    volumes:
      - flink-jobmanager-job-artifacts:/opt/flink/artifacts
    command: jobmanager
    networks:
      - kafkaesque
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/overview"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 120s

  flink-taskmanager:
    image: flink:1.18-java17
    container_name: flink-taskmanager
    hostname: flink-taskmanager
    depends_on:
      flink-jobmanager:
        condition: service_healthy
      broker:
        condition: service_started
    environment:
      - FLINK_ENV=java17
    volumes:
      - ./flink-conf/taskmanager.yaml:/opt/flink/conf/flink-conf.yaml:ro
    command: taskmanager
    networks:
      - kafkaesque

  flink-analytics:
    build:
      context: ./flink-analytics
      dockerfile: Dockerfile
    image: flink-analytics:latest
    container_name: flink-analytics
    depends_on:
      flink-jobmanager:
        condition: service_healthy
      broker:
        condition: service_started
      schema-registry:
        condition: service_started
    volumes:
      - ./flink-conf/analytics.yaml:/opt/flink/conf/flink-conf.yaml:ro
    networks:
      - kafkaesque

volumes:
  postgres-data:
  flink-jobmanager-job-artifacts:

networks:
  kafkaesque:
    name: kafkaesque
