services:
  postgres:
    container_name: postgres
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=orders
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - kafkaesque
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  topic-register:
    container_name: topic-register
    build:
      context: .
      dockerfile: Schemas/TopicRegister/Dockerfile
    environment:
      - DOTNET_ENVIRONMENT=Development
      - Kafka__BootstrapServers=broker:29092
    depends_on:
      broker:
        condition: service_started
    restart: "no"
    networks:
      - kafkaesque

  schema-register:
    container_name: schema-register
    build:
      context: .
      dockerfile: Schemas/SchemaRegister/Dockerfile
    environment:
      - DOTNET_ENVIRONMENT=Development
      - Kafka__BootstrapServers=broker:29092
      - Kafka__SchemaRegistryUrl=http://schema-registry:8081
    depends_on:
      topic-register:
        condition: service_completed_successfully
      schema-registry:
        condition: service_started
    restart: "no"
    networks:
      - kafkaesque

  fulfillment-service:
    container_name: fulfillment-service
    build:
      context: .
      dockerfile: FulfillmentService/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_URLS=http://+:8080
      - Kafka__BootstrapServers=broker:29092
      - Kafka__SchemaRegistryUrl=http://schema-registry:8081
      - GROUP_ID=test-1
    ports:
      - "5054:8080"
    networks:
      - kafkaesque
    depends_on:
      schema-register:
        condition: service_completed_successfully

  order-service:
    container_name: order-service
    build:
      context: .
      dockerfile: OrderService/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_URLS=http://+:8080
      - Kafka__BootstrapServers=broker:29092
      - Kafka__SchemaRegistryUrl=http://schema-registry:8081
      - ConnectionStrings__OrdersDatabase=Host=postgres;Port=5432;Database=orders;Username=postgres;Password=postgres
    ports:
      - "5295:8080"
    networks:
      - kafkaesque
    depends_on:
      postgres:
        condition: service_healthy
      schema-register:
        condition: service_completed_successfully

  flink-job-manager:
    image: flink:1.18-java17
    container_name: flink-job-manager
    hostname: flink-job-manager
    ports:
      - "8084:8081"
    depends_on:
      broker:
        condition: service_started
    environment:
      - FLINK_ENV=java17
      - FLINK_PROPERTIES=jobmanager.rpc.address:flink-job-manager
    command: jobmanager
    networks:
      - kafkaesque
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/overview"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 120s

  flink-task-manager:
    image: flink:1.18-java17
    container_name: flink-task-manager
    hostname: flink-task-manager
    depends_on:
      flink-job-manager:
        condition: service_healthy
      broker:
        condition: service_started
      schema-registry:
        condition: service_started
    environment:
      - FLINK_ENV=java17
    volumes:
      - ./flink-conf/taskmanager.yaml:/opt/flink/conf/flink-conf.yaml:ro
    command: taskmanager
    networks:
      - kafkaesque

  flink-job-submitter:
    build:
      context: ./flink-job-submitter
      dockerfile: Dockerfile
    image: flink-job-submitter:latest
    container_name: flink-job-submitter
    depends_on:
      flink-job-manager:
        condition: service_healthy
      broker:
        condition: service_started
      schema-registry:
        condition: service_started
    volumes:
      - ./flink-conf/jobmanager.yaml:/opt/flink/conf/flink-conf.yaml:ro
    networks:
      - kafkaesque

volumes:
  postgres-data:

networks:
  kafkaesque:
    name: kafkaesque
